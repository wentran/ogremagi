
export default function surveyHandler (state=[], action){
  switch(action.type){
   case  'POST_SURVEY_RESPONSES':
   axios.post('/sendMessage', {
     user: {
       message: action.payload
     }
   })
   .then(function(response){
     console.log('saved successfully')
   })
  }
}
